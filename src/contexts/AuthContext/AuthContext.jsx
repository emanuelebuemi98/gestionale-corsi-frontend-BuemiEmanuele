//importo il metodo per creare il mio contesto
import { createContext } from "react";

//definisco una costante a cui assegno il valore di ritorno del metodo createContect con un oggetto che restituisce il contesto di default
export const AuthContext = createContext({});